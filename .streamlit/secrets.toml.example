# Скопируйте этот файл в .streamlit/secrets.toml и подставьте реальные значения.
# .streamlit/secrets.toml НЕ должен попадать в git (добавьте его в .gitignore).

[cdse]
client_id = "YOUR_CDSE_CLIENT_ID"
client_secret = "YOUR_CDSE_CLIENT_SECRET"
